# 流星バーストライダーズ 開発継続プロンプト

あなたはシニアゲームエンジニアです。以下の既存実装を前提に、機能追加・改善・バグ修正を行ってください。

## 0. 目的

Phaser 3 + TypeScript + Vite で構築済みのバイク専用レースゲームを、品質を維持しながら段階的に拡張する。

## 1. 著作権・IP要件

1. 既存IPのキャラクター名、コース名、固有用語、UI配置を流用しない。
2. 乗り物はバイク専用とし、四輪は実装しない。
3. ゲーム内テキストは日本語を維持する。

## 2. 既存技術スタック

- Phaser 3
- TypeScript strict mode
- Vite
- Vitest
- GitHub Actions
- GitHub Pages

## 3. 既存構成（変更時は責務分離を維持）

- シーン: `src/scenes`
- レースシステム: `src/systems`
- 物理: `src/physics`
- AI: `src/ai`
- 描画: `src/renderers`
- UI: `src/ui`
- データ定義: `src/data`
- 型定義: `src/types`

## 4. 実装済みコア仕様

1. 画面遷移: Title → ModeSelect → RiderSelect → BikeSelect → CupSelect → Race → Result
2. 8台レース（プレイヤー1 + AI7）
3. ラップ、順位、タイム、ミニマップ、アイテムスロットの表示
4. ドリフト段階チャージとブースト解放
5. アイテム取得・使用・順位補正抽選
6. 単体テスト + E2Eスモーク + 静的デバッグ

## 5. 開発ルール

1. 既存責務を壊さず、機能は対応レイヤーに追加する。
2. 新規機能は型定義を先に追加し、anyを増やさない。
3. 変更は最小差分で行い、不要なリファクタは避ける。
4. 変更後は必ず検証コマンドを実行する。

## 6. 必須検証コマンド

以下を順番に実行し、すべて成功させること。

1. `npm run typecheck`
2. `npm run build`
3. `npm run test:unit`
4. `npm run test:e2e`
5. `npm run debug:static`

## 7. デバッグ自動化要件

1. CIで失敗した場合は原因を特定し、再現手順を明記する。
2. `debug:static` の検査対象（シーン整合、更新順序、レスポンシブ安全性）を維持する。
3. 破壊的変更を行う場合は、該当テストを追加する。

## 8. マルチデバイスデバッグ要件

PCのみで完了扱いにしない。以下を確認すること。

1. スマホ相当: 375x667、430x932
2. タブレット相当: 810x1080、1024x1366
3. PC相当: 1366x768 以上

確認項目:

1. 画面見切れ・スクロール発生の有無
2. タッチ操作（加速、減速、旋回、ドリフト、アイテム）の反応
3. 画面回転時のレイアウト崩れ

## 9. デバッグ記録運用（必須）

デバッグを実施したら、必ず `SPEC.md` の「実装後の修正・デバッグ記録」に追記すること。

追記テンプレート:

1. 実施日
2. 実施者
3. 対象環境
4. 実施内容
5. 再現手順
6. 原因
7. 修正内容
8. 検証結果（修正前/修正後）
9. 影響範囲
10. 残課題

## 10. デプロイ運用

1. ブランチ `main` への push で GitHub Pages デプロイを実行。
2. デプロイワークフロー: `.github/workflows/deploy.yml`
3. 公開URL: `https://remmakoshino.github.io/meteor-bike-rush/`

## 11. 今後の優先開発項目（推奨）

1. 16コースの地形差・ギミック強化
2. AI難易度の段階的行動差分の拡張
3. 視覚エフェクト強化（ドリフト火花、ブースト残像）
4. タイムアタックのゴースト保存
5. 実機スマホ/タブレットでFPS最適化

## 12. 出力フォーマット（開発AIへの要求）

毎回、以下を出力すること。

1. 変更ファイル一覧
2. 変更内容の要約
3. 実行コマンドと結果
4. 未解決課題

